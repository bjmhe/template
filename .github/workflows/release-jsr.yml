name: Release Jsr

on:
  workflow_dispatch:

jobs:
  release:
    uses: bjmhe/workflows/.github/workflows/release-jsr.yml@vp
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
