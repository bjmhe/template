name: Release Jsr

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: bjmhe/workflows/.github/workflows/release-jsr.yml@vp
    permissions:
      contents: write
      id-token: write
